好的，以下是一份可直接交给 Trae 执行的详细修改大纲，整合了专家版 API 修正和横竖屏布局适配，确保目录树在横竖屏下都能正常显示。

---

📁 修改大纲：GitHub Explorer 目录树横竖屏适配 + API 优化

一、修改 AndroidManifest.xml

文件路径：app/src/main/AndroidManifest.xml

操作：找到 RepoDetailActivity 的 <activity> 标签，添加 android:configChanges 属性（如果已有则跳过）。

替换为：

```xml
<activity
    android:name=".ui.repodetail.RepoDetailActivity"
    android:configChanges="orientation|screenSize|keyboardHidden"
    android:exported="false" />
```

---

二、修改布局文件 fragment_file_tree.xml

文件路径：app/src/main/res/layout/fragment_file_tree.xml

操作：无需修改（已经包含 rootLayout、treeContainer 等必要 id），但请确认根布局已添加 android:id="@+id/rootLayout"。

检查点：确保存在以下 id：

· @+id/rootLayout
· @+id/treeContainer
· @+id/expandableTreeView
· @+id/fileContentScrollView
· @+id/emptyHintText

---

三、修改 FileTreeFragment.kt

文件路径：app/src/main/java/com/example/githubexplorer/ui/repodetail/FileTreeFragment.kt

3.1 优化 loadRepoTree()（专家版修正）

定位：loadRepoTree() 函数内部，找到以下代码块（约第 113-120 行）：

```kotlin
val branchRef = RetrofitProvider.api.getBranchRef(owner, repo, branch)
val commitSha = branchRef.`object`.sha
val treeResp = RetrofitProvider.api.getTree(owner, repo, commitSha, 1)
```

替换为（直接使用分支名请求 tree）：

```kotlin
// 直接使用分支名请求 tree，不再通过 getBranchRef
val treeResp = RetrofitProvider.api.getTree(owner, repo, branch, 1)
```

完整替换后的 loadRepoTree() 函数（可直接复制替换原函数）：

```kotlin
private fun loadRepoTree() {
    val activity = requireActivity() as RepoDetailActivity
    val owner = activity.owner
    val repo = activity.repo
    binding.progressBar.visibility = View.VISIBLE
    lifecycleScope.launch {
        try {
            val repoInfo = RetrofitProvider.api.getRepo(owner, repo)
            val branch = repoInfo.defaultBranch ?: "main"
            activity.branch = branch

            // 直接使用分支名请求 tree，不再通过 getBranchRef 获取 commit SHA
            val treeResp = RetrofitProvider.api.getTree(owner, repo, branch, 1)
            treeNodes.clear()
            treeNodes.addAll(treeResp.tree)
            buildTree()
        } catch (e: Exception) {
            Toast.makeText(context, "Failed to load tree: ${e.message}", Toast.LENGTH_LONG).show()
        } finally {
            binding.progressBar.visibility = View.GONE
        }
    }
}
```

3.2 完善 updateLayoutOrientation()（横竖屏适配）

定位：找到 updateLayoutOrientation() 函数（如果没有则新增）。

完整替换为以下代码（处理顶部栏、分割线、树容器和内容预览区域）：

```kotlin
private fun updateLayoutOrientation() {
    if (!::rootLayout.isInitialized) return
    val isLandscape = resources.configuration.orientation == Configuration.ORIENTATION_LANDSCAPE

    // 1. 调整根布局方向
    rootLayout.orientation = if (isLandscape) LinearLayout.HORIZONTAL else LinearLayout.VERTICAL

    // 2. 处理顶部按钮栏（索引0）和分割线（索引1）
    val topBar = rootLayout.getChildAt(0)
    val divider = rootLayout.getChildAt(1)

    if (isLandscape) {
        (topBar.layoutParams as LinearLayout.LayoutParams).apply {
            width = LinearLayout.LayoutParams.WRAP_CONTENT
            height = LinearLayout.LayoutParams.MATCH_PARENT
            weight = 0f
        }
        (divider.layoutParams as LinearLayout.LayoutParams).apply {
            width = LinearLayout.LayoutParams.WRAP_CONTENT
            height = LinearLayout.LayoutParams.MATCH_PARENT
            weight = 0f
        }
    } else {
        (topBar.layoutParams as LinearLayout.LayoutParams).apply {
            width = LinearLayout.LayoutParams.MATCH_PARENT
            height = LinearLayout.LayoutParams.WRAP_CONTENT
            weight = 0f
        }
        (divider.layoutParams as LinearLayout.LayoutParams).apply {
            width = LinearLayout.LayoutParams.MATCH_PARENT
            height = LinearLayout.LayoutParams.WRAP_CONTENT
            weight = 0f
        }
    }

    // 3. 调整树容器和内容预览区域
    val treeParams = binding.treeContainer.layoutParams as LinearLayout.LayoutParams
    val scrollParams = binding.fileContentScrollView.layoutParams as LinearLayout.LayoutParams

    if (isLandscape) {
        treeParams.width = 0
        treeParams.height = LinearLayout.LayoutParams.MATCH_PARENT
        treeParams.weight = 1f

        scrollParams.width = 0
        scrollParams.height = LinearLayout.LayoutParams.MATCH_PARENT
        scrollParams.weight = 1f
    } else {
        treeParams.width = LinearLayout.LayoutParams.MATCH_PARENT
        treeParams.height = 0
        treeParams.weight = 1f

        scrollParams.width = LinearLayout.LayoutParams.MATCH_PARENT
        scrollParams.height = 0
        scrollParams.weight = 1f
    }
    binding.treeContainer.layoutParams = treeParams
    binding.fileContentScrollView.layoutParams = scrollParams

    // 4. 调整空提示文本
    (binding.emptyHintText.layoutParams as LinearLayout.LayoutParams).apply {
        width = if (isLandscape) LinearLayout.LayoutParams.WRAP_CONTENT else LinearLayout.LayoutParams.MATCH_PARENT
        weight = 0f
    }

    // 5. 请求重新布局
    rootLayout.requestLayout()
}
```

3.3 确保在 onViewCreated 和 onConfigurationChanged 中调用

定位：onViewCreated 末尾添加调用（如果已有则保留）：

```kotlin
override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
    super.onViewCreated(view, savedInstanceState)
    binding.downloadRepoButton.setOnClickListener { downloadRepo() }
    loadRepoTree()
    updateLayoutOrientation()   // 添加这一行
}
```

定位：重写 onConfigurationChanged（如果没有则新增）：

```kotlin
override fun onConfigurationChanged(newConfig: Configuration) {
    super.onConfigurationChanged(newConfig)
    updateLayoutOrientation()
}
```

3.4 添加必要的导入（如果缺失）

在文件顶部导入区域添加：

```kotlin
import android.content.res.Configuration
import android.widget.LinearLayout
```

---

四、验证步骤

1. 编译运行：确保无编译错误。
2. 登录：使用有效的 GitHub Token。
3. 选择一个仓库，进入 Files 标签页。
4. 检查竖屏：目录树在上，文件预览在下，目录树应正常显示并可展开/点击。
5. 旋转为横屏：布局应自动变为左右分屏（左侧目录树，右侧文件预览），两者平分宽度，顶部按钮栏紧凑显示在左上方。
6. 点击文件：右侧应显示文件内容（文本文件可预览，二进制文件提示无法预览）。
7. 旋转回竖屏：布局恢复正常上下排列。

---

✅ 总结

以上修改解决了两个核心问题：

1. API 调用优化：移除多余的 getBranchRef，直接使用分支名获取 Git Tree，避免 404 错误。
2. 横竖屏自适应布局：动态调整根布局方向及子视图参数，确保目录树和预览区域在横屏时左右分屏，竖屏时上下分屏。

请 Trae 按此大纲依次执行修改，完成后通知用户进行验证。